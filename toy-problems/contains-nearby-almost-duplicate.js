/**
 * @param {number[]} nums
 * @param {number} k
 * @param {number} t
 * @return {boolean}
 */
var containsNearbyAlmostDuplicate = function(nums, k, t) {
    // t = delta of values at indices
    // k = delta of indices
    // conditions: nums[i] - nums[j] (abs) <= t
    //             i - j (abs) <= k
    
};
